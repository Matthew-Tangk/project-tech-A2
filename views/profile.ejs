<%- include('./partials/head') %>

<body id="yourProfile">

  <%- include('./partials/header') %>

  <main>
    <section>
      <div>
        <p> Welcome </p>
        <h2> <%= profileData.username %> </h2>
        <P> <%= profileData.age %> </P>
        <P> <%= profileData.about %></P>
      </div>

      <img width="100px" src="/static/img/profilepic/<%= profileData.file %>"  alt="Profile Picture"/>
    </section>
    
    <section>
      <ul>
        <% selectedGenre.forEach(selectedGenre => { %>
          <li>
            <img src="../static/img/genres/<%= selectedGenre.img %>" alt="<%= selectedGenre.name %>">
            <p><%= selectedGenre.name %></p>
          </li>
          <% }); %>
      </ul>
    </section>

    <section>
      <% if(favoriteArtists == null) { %>
        <p> My artists</p>
        <section id="emptyState">
          <p>Complete your profile</p>
          <a href="/profile/add-artists">Add artists</a>
        </section>

      <% } else { %>
        <section>
          <ul>
            <% additionFavoriteArtistsData.forEach(artist => { %>
              <li>
                <img src="<%= artist.path %>" alt="<%= artist.name %>">
                <%= artist.name %>
              </li>
            <% }) %>
          </ul>

          <a href="/profile/add-artists">Edit artists</a>
        </section>
      <% } %>

    </section>
  </main>
</body>
</html>