<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="author" content="Jeannet Welboren" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%=title%></title>
 
    <link rel="stylesheet" href="/css/style.css" />
    <% if (title === 'Concert buddies' || title === 'Personal info' || title ===
    'Upload photo' || title === 'Add bio' || title === 'Add genres' || title ===
    'My profile') { %>
    <link rel="stylesheet" href="/css/registration.css" />
    <% } %> <% if (title === 'Add artists') { %>
    <link rel="stylesheet" href="/css/addartists.styles.css" />
    <% } %> <% if (title === 'My events') { %>
    <link rel="stylesheet" href="/css/my-events.css" />
    <% } %> <% if (title === 'Invites') { %>
    <link rel="stylesheet" href="/css/invites.styles.css" />
    <% } %> <% if (title === 'Event details') { %>
    <link rel="stylesheet" href="/css/events-details.css" />
    <% } %>

    <% if (title === 'Upcoming events') { %>
      <link rel="stylesheet" href="/css/upcoming-events.css">
      <style>
        /* Dynamically set the background image for each scroll item (done inline ) */
        <% events.forEach((event, index)=> {
            %> .scroll-item:nth-of-type(<%=index + 1 %>) {
              background-image: url('<%= event.background %>');
              background-size: cover;
            }
    
            <%
          }) %>
      </style>
    <% } %>


  </head>
</html>
